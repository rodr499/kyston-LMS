# Copy to .env.local and fill in values.

# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
# Database: use Supabase Connection pooler in TRANSACTION mode to avoid "max clients reached".
# Supabase Dashboard → Project Settings → Database → Connection string → "Connection pooling" → Transaction.
# Use the URI that includes port 6543 (pooler). Do not use Session mode (port 5432) with Next.js.
# No quotes around the value; password with special chars must be URL-encoded (e.g. @ → %40).
DATABASE_URL=

# App
NEXT_PUBLIC_APP_DOMAIN=kyston.org

# Subdomain automation (Vercel only). Dynu uses wildcard; we add/remove domains in Vercel when tenants are created/deleted.
VERCEL_TOKEN=
VERCEL_PROJECT_ID=
VERCEL_TEAM_ID=
VERCEL_IP=216.198.79.1
# CNAME target for custom domains (Vercel project-specific: Project Settings → Domains → Add domain → CNAME value)
VERCEL_CNAME=a39bc0afdae1b833.vercel-dns-017.com
# Shared OAuth callback base (no tenant subdomain). Dev: http://localhost:3000  Prod: https://kyston.org
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Stripe
STRIPE_SECRET_KEY=
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
STRIPE_WEBHOOK_SECRET=

# Resend
RESEND_API_KEY=

# Zoom
ZOOM_CLIENT_ID=
ZOOM_CLIENT_SECRET=

# Google (for Google Meet / Calendar)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Microsoft (for Teams + facilitator sync from 365). In Azure App registration:
#   Authentication: add redirect URI (Dev: http://localhost:3000/api/integrations/teams/callback, Prod: https://<domain>/api/integrations/teams/callback)
#   API permissions (delegated): OnlineMeetings.ReadWrite, User.Read, Group.Read.All (for syncing facilitators from a 365 group)
MICROSOFT_CLIENT_ID=
MICROSOFT_CLIENT_SECRET=
